# Trabajo diferido

Hallazgos reales pero no accionables ahora, registrados para no perderlos.

## Deferred from: code review of story-7.1 (2026-07-10)

- **Parent con span-id sintético → posible nodo huérfano en el waterfall** (blind, BAJA) — cuando la correlación se hace desde un trace-id de 32 hex (caso real de todos los emisores actuales), `ActividadHotelBookingHub.ContextoDesde` fabrica un `ActivityContext` con un `SpanId` **aleatorio**. El span del consumidor agrupa por `TraceId` correctamente, pero su `ParentSpanId` apunta a un span inexistente → algunos backends (Jaeger/Tempo/Aspire) pueden renderizarlo como nodo huérfano en vez de colgarlo del span originante. Consecuencia directa del transporte Dapr diferido: al cablear Dapr, el sidecar propaga el `traceparent` completo (con el span-id padre real) y el nodo cuelga limpiamente. `[Comun/Observabilidad/ActividadHotelBookingHub.cs]`

## Deferred from: Story 7.1 (traza distribuida, 2026-07-10)

- **Salto físico por sidecar Dapr no cableado (propagación de `traceparent` en el CloudEvent)** (alcance, informativo) — el transporte Dapr pub/sub sigue diferido en todo el sistema (E1/E5): el publicador es `PublicadorEventosLog` (placeholder) y el `Worker` no tiene suscripción de transporte real. La correlación asíncrona de 7.1 se hace **Dapr-ready** por el `trace-id` de negocio del envelope (span de consumidor bajo la misma traza), pero el `traceparent` completo (con el span-id padre real) no viaja hasta que se cablee Dapr. Al cablear Dapr, el sidecar propaga el `traceparent` en el CloudEvent y la correlación manual de `DespachadorNotificaciones`/`ActividadHotelBookingHub.IniciarConsumo` se puede retirar. `[Notificaciones.Worker/Notificaciones/DespachadorNotificaciones.cs, Comun/Observabilidad/ActividadHotelBookingHub.cs]`
- **Evidencia del dashboard de Aspire capturada manualmente, no en CI** (alcance, informativo) — AC-E7.1.1 (propagación HTTP Gateway→servicio) y AC-E7.1.5 (health fuera de la traza) se satisfacen por la instrumentación OTel por defecto (AspNetCore+HttpClient) y el filtro de health ya existente en `ServiceDefaults`; la prueba en CI cubre emisión del span de negocio y correlación asíncrona con `ActivityListener`. La visualización del waterfall multi-servicio requiere levantar el `AppHost` (Docker) y abrir el dashboard — documentada en `docs/observabilidad.md`, no automatizada. `[docs/observabilidad.md]`

## Deferred from: code review of story-6.2 (2026-07-10)

- **Detalle de reserva `SoloAgente` mientras el Viajero puede crear/solicitar** (blind, MED) — un Viajero que crea una reserva (`AgenteOViajero`) recibe 403 al releerla por `GET /api/v1/reservas/{id}` (`SoloAgente`). Es **per-spec** (FR-13: listado/detalle es capacidad del agente para conciliar comisiones; no hay FR de "viajero consulta su reserva por id" — la recibe en el 201). Sobre-restricción segura, no escalada. Si producto pide una vista de reserva para el viajero, historia propia. `[Reservas.Api/Program.cs]`
- **`RequireRole` sin normalización de caso ni soporte de rol CSV/array → 403 con IdP OIDC externo** (edge, BAJA) — `IsInRole` compara ordinal/case-sensitive; un IdP que emita `"agente"` o roles como un único claim `"Agente,Viajero"` caería en 403. Fail-closed hoy (emisor propio consistente). Cerrar al integrar un IdP real (F2): normalizar el caso y/o expandir un claim de rol multivaluado. `[Comun.Web/Seguridad/AutorizacionPorRolExtensions.cs]`

## Deferred from: code review of story-6.1 (2026-07-10)

- **Clave HMAC simétrica compartida por Gateway + servicios** (blind, MED-diseño) — con HS256 la misma `Jwt:SigningKey` firma y valida, y los tres componentes (gateway/hoteles/reservas) la comparten. Si se filtra desde cualquiera, o un servicio se compromete, se pueden forjar tokens con cualquier rol/identidad aceptados por todos. Riesgo **aceptado** en el alcance de la prueba (JWT/OIDC propio, sin IdP externo). Producción: migrar a claves **asimétricas** (RS256/ES256) con la clave privada de firma aislada del validador, o un IdP (Keycloak/Entra) con JWKS. `[deploy/docker-compose.yml, AutenticacionJwtExtensions.cs]`
- **Autorización opt-in en servicios vs secure-by-default → Story 6.2** (blind, BAJA-MED) — el Gateway es secure-by-default (`MapReverseProxy().RequireAuthorization()`), pero los `*.Api` decoran ruta por ruta; un endpoint nuevo sin `.RequireAuthorization()` quedaría anónimo (servicios expuestos en 8081/8082). 6.2 (RBAC) consolidará el modelo por endpoint y debería adoptar `FallbackPolicy` requiriendo autenticación + `AllowAnonymous` explícito en `/health`, `/alive`. `[Hoteles.Api/Program.cs, Reservas.Api/Program.cs]`
- **Naming del claim de rol (`ClaimTypes.Role` URI vs `role`) → Story 6.2** (auditor, INFO) — `JwtTestFactory.EmitirToken` emite el rol con `ClaimTypes.Role` (se serializa como URI larga `.../claims/role`); el README documenta el claim corto `role`. Funciona con `User.IsInRole` (RoleClaimType por defecto = `ClaimTypes.Role`), pero una policy de 6.2 que espere `role` corto tendría fricción. 6.2 fija el `RoleClaimType`/claim canónico y alinea emisor + README. `[tests/Seguridad.FunctionalTests/JwtTestFactory.cs, README.md]`

## Deferred from: code review of story-5.3 (2026-07-09)

- **D5 · FR-21 "(y al agente)" en la resolución no implementado** (auditor, BAJA) — FR-21 dice que al resolverse se notifica "al viajero (y al agente)", pero 5.3 solo añadió `HuespedEmail` a los eventos de resolución (los AC-E5.3.1/.2 solo exigen al viajero; decisión YAGNI). A diferencia de 5.1a (confirmación) que sí avisa a huésped+agente. Confirmar con producto (John/Winston) si la resolución debe avisar también al agente; si sí, añadir `AgenteEmail` (aditivo) a `ReservaCancelada.v1`/`SolicitudCancelacionRechazada.v1` + copy del agente en `ConsumidorResolucionCancelacion`. `[Comun.Eventos/ReservaCanceladaV1.cs, SolicitudCancelacionRechazadaV1.cs; Notificaciones.Worker/.../ConsumidorResolucionCancelacion.cs]`
- **D2 (recurrente, ahora observable) · Doble notificación del atajo de un paso** — con 5.3 implementado, el atajo 4.3 (que emite `SolicitudCancelacionRegistrada` + la resolución) haría que el viajero reciba el acuse "estimada/por resolver" (5.2) Y el desenlace (5.3) casi simultáneos y contradictorios (peor en rechazo: el acuse insinúa que procede, el desenlace dice "rechazada, sigue Confirmada"). Ver el detalle en el review de 5.2. Resolver al cablear el transporte: suprimir el acuse de solicitud cuando el evento proviene del atajo (marca/correlación) o ajustar copy. `[Reservas.Application/.../CancelarEnUnPasoCommandHandler.cs, Notificaciones.Worker/.../ConsumidorSolicitudCancelacion.cs]`

## Deferred from: code review of story-5.2 (2026-07-09)

- **D1 · Enrutamiento multi-consumidor por tipo de evento** (blind, ALTA→latente) — con dos consumidores (`ConsumidorReservaConfirmada`, `ConsumidorSolicitudCancelacion`) y un `DespachadorNotificaciones` que inyecta UN solo `IProcesadorEvento`, no hay forma de despachar por tipo. Registrar ambos como `IProcesadorEvento` rompería el despachador (resolvería el último). Al cablear el transporte real (Dapr pub/sub), añadir un router por `evento.Type` (o `IEnumerable<IProcesadorEvento>` con fan-out y filtrado por tipo). Hoy no hay pump → no es bug en runtime, pero la funcionalidad de 5.2 no se dispararía end-to-end sin esto. `[Notificaciones.Worker/Program.cs, DespachadorNotificaciones.cs]`
- **D2 · Atajo de un paso (4.3) genera acuse "por resolver" para algo ya resuelto** (edge, MED) — `CancelarEnUnPasoCommandHandler` emite `SolicitudCancelacionRegistrada.v1` Y la resolución en la misma tx (audit trail, AC-E4.3.1). El consumidor 5.2 reacciona al primero sin distinguir origen → el viajero recibiría "penalidad estimada, te avisaremos cuando se resuelva" seguido casi de inmediato del correo de resolución (5.3) → contradictorio/duplicado. Decidir en 5.3: suprimir el acuse de solicitud cuando el evento proviene del atajo, o ajustar el copy. `[Reservas.Application/.../CancelarEnUnPasoCommandHandler.cs, Notificaciones.Worker/.../ConsumidorSolicitudCancelacion.cs]`
- **D3 · "Huésped principal = primero de la colección" sin orden garantizado** (blind+edge, BAJA) — los emisores toman `reserva.Huespedes.FirstOrDefault()?.Email`; la owned collection (`OwnsMany`) no tiene `OrderBy` explícito y el dominio no tiene noción de titular. En reservas multi-huésped el acuse podría ir a un acompañante. Introducir un "huésped titular" (flag/orden) si el negocio lo exige. Mismo supuesto que `ReservaConfirmada.v1` (5.1a). `[Reservas.Application/.../SolicitarCancelacionCommandHandler.cs, CancelarEnUnPasoCommandHandler.cs]`
- **D4 · Skip silencioso de destinatario nulo sin observabilidad** (edge+auditor, BAJA) — `EnvioIdempotenteCorreos.EnviarLoteAsync` hace `continue` ante un destinatario nulo/vacío sin log; si ambos son nulos, el evento se procesa "con éxito" sin enviar nada ni rastro → una falla de ruteo es indistinguible de éxito. Añadir log de advertencia al omitir (junto con la observabilidad del dead-letter/transporte). `[Notificaciones.Worker/.../EnvioIdempotenteCorreos.cs]`

## Deferred from: code review of story-5.1b (2026-07-09)

La mayoría depende de cablear el transporte real productor→worker (hoy diferido en todo el sistema) y/o de dar al tope de intentos la misma multi-instancia que el inbox.

- **F4 · `InboxIdempotenciaEnMemoria` sin TTL/evicción** (blind+edge, MED) — cada efecto exitoso deja su clave residente para siempre en el `ConcurrentDictionary` → crecimiento de memoria no acotado en un worker de larga vida, y sin la autocorrección at-most-once que da el TTL de Redis. Es el fallback de desarrollo (sin `redis` configurado); el camino real es `InboxIdempotenciaRedis`. Al endurecer el fallback: cap de tamaño + evicción por antigüedad o `MemoryCache` con expiración. `[Notificaciones.Worker/Notificaciones/InboxIdempotenciaEnMemoria.cs]`
- **F5 · Redis caído se contabiliza como intento → falso veneno** (edge, MED) — si `IntentarMarcarProcesadoAsync` lanza `RedisConnectionException`, el `DespachadorNotificaciones` lo cuenta como intento de procesamiento; un outage de Redis más largo que `MaxIntentos` re-entregas mandaría a dead-letter mensajes válidos. Distinguir fallo de infraestructura (no cuenta, reintento sin cota con backoff) de mensaje-veneno (cuenta). Latente: el despachador aún no se invoca en runtime. `[Notificaciones.Worker/Notificaciones/DespachadorNotificaciones.cs]`
- **F6 · Race check-then-act en el tope → dead-letter duplicado** (blind, MED) — `IncrementarAsync` es atómico, pero el `if (intentos >= MaxIntentos)` posterior no lo es respecto a entregas concurrentes: dos competing-consumers pueden apartar el mismo mensaje dos veces. Se cierra al pasar a un contador Redis con decisión atómica (ver F7). `[Notificaciones.Worker/Notificaciones/DespachadorNotificaciones.cs]`
- **F7 · Sin variante Redis del contador de reintentos** (auditor+edge, MED) — `IContadorReintentos` solo tiene `ContadorReintentosEnMemoria`; el inbox sí es multi-instancia (Redis). Con `redis` configurado y N réplicas, un veneno se reintenta hasta `MaxIntentos×N` y el dead-letter no es consistente. Añadir `ContadorReintentosRedis` (`INCR`+`EXPIRE`) y registrarlo junto al inbox Redis. `[Notificaciones.Worker/Notificaciones/IContadorReintentos.cs, Program.cs]`
- **F8 · Fallo del sink de dead-letter re-bloquea** (edge, MED-BAJA) — si `IColaDeadLetter.EnviarAsync` lanza, no se hace `ReiniciarAsync` ni ACK → el veneno vuelve a re-entregarse y el contador crece sin cota. Necesita política de fallback (reintento acotado del propio dead-letter / sink de último recurso). `[Notificaciones.Worker/Notificaciones/DespachadorNotificaciones.cs]`
- **F9 · Caída del worker entre reservar y enviar** (auditor, MED) — ventana de pérdida acotada por el TTL (24 h por defecto), documentada como límite inherente en `IInboxIdempotencia`. AC-E5.1b.2 (broker caído, worker vivo) sí queda a 0 pérdida y probado; este otro modo de fallo (crash del proceso mid-efecto) no se ejercita por test. Mitigación futura: TTL más corto para la reserva "en curso" + confirmación tras el envío (marca definitiva), o transacción de saga. `[Notificaciones.Worker/Notificaciones/ConsumidorReservaConfirmada.cs]`
- **F10 · PII en logs del dead-letter** (blind, BAJA) — `motivo = ex.Message` se registra tal cual; las excepciones del adaptador SMTP suelen incluir la dirección del destinatario. Sanear o registrar solo tipo/categoría del error. `[Notificaciones.Worker/Notificaciones/ColaDeadLetterLog.cs]`
- **F11 · `CancellationToken` no propagado a Redis** (blind, BAJA) — solo `ThrowIfCancellationRequested` al entrar; `StringSetAsync`/`KeyDeleteAsync` no reciben el token (SE.Redis no lo soporta directo). Un Redis lento no cancela durante shutdown. Usar timeouts de comando/`CommandFlags` si se requiere. `[Notificaciones.Worker/Notificaciones/InboxIdempotenciaRedis.cs]`
- **F14 · `DespachadorNotificaciones` registrado pero nunca invocado** (auditor+edge, BAJA) — no hay loop de suscripción que llame a `DespacharAsync`; la protección de veneno solo existe en tests. Mismo límite que el consumidor 5.1a (transporte Dapr placeholder en todo el sistema). Cablear al conectar el transporte real. `[Notificaciones.Worker/Program.cs, Worker.cs]`
- **F16 · Contador de reintentos no persistente** (edge, BAJA) — el diccionario en memoria se pierde al reiniciar el proceso; un veneno que provoque crash reinicia su conteo → puede no llegar nunca a dead-letter (crash-loop). Se cierra con el contador Redis (F7) con TTL adecuado. `[Notificaciones.Worker/Notificaciones/ContadorReintentosEnMemoria.cs]`

## Deferred from: code review of story-5.1a (2026-07-09)

- **Transporte real productor→worker (suscripción Dapr) sin cablear** (edge+auditor) — el `ConsumidorReservaConfirmada` está registrado en el DI del worker pero ningún transporte lo invoca (el `Worker.ExecuteAsync` sigue en latido). El AC-E5.1a.1 se verifica en test/demo; el transporte Dapr es placeholder en TODO el sistema (`PublicadorEventosLog` solo loguea; E3 `ProyectorCatalogo` también solo se invoca en tests). Cablear la suscripción real (Dapr pub/sub o alternativa) es un ítem de infra transversal — posible party-mode al abordarlo. `[Notificaciones.Worker/Program.cs, Worker.cs; *.Infrastructure/Mensajeria/PublicadorEventosLog.cs]`
- **Exactamente-una-vez + fallo parcial de los 2 correos → 5.1b** (blind+edge, MED) — los 2 `NotificarAsync` son secuenciales sin dedup: si el 2º falla (adaptador real), el 1º ya salió y una re-entrega duplica. Es el núcleo de 5.1b (idempotencia por `MessageId`, at-least-once). `[Notificaciones.Worker/.../ConsumidorReservaConfirmada.cs]`
- **Mensaje-veneno sin dead-letter → 5.1b Task 4** (blind, MED) — una `JsonException` de un payload corrupto propaga sin tope de intentos ni dead-letter; con un pump real bloquearía el stream. `[Notificaciones.Worker/.../ConsumidorReservaConfirmada.cs]`

## Deferred from: code review of story-4.3 (2026-07-09)

- **`MD5.HashData` en la derivación de MessageId podría lanzar bajo política FIPS** (blind, BAJA) — `ColaOutbox.DerivarMessageId` usa MD5 (solo como id de dedup determinista, NO seguridad). En un host con FIPS mode habilitado, las primitivas MD5 pueden lanzar. Entorno actual estándar (no FIPS). Si se exige FIPS, cambiar a un hash no-criptográfico determinista (FNV/xxHash) o a `Guid.CreateVersion5` cuando esté disponible. `[Reservas.Infrastructure/Mensajeria/ColaOutbox.cs]`
- **Estabilidad de MessageId ante 1205 forzado sin test E2E** (auditor, BAJA) — la estabilidad (semilla fija + `ReiniciarConteo` por intento + dedup por `UNIQUE(MessageId)`) está probada a nivel unitario (`Los_MessageId_derivados_son_estables_entre_reintentos`), pero no con un `SqlException` 1205 real que reintente un comando multi-evento y asevere exactamente 2 filas (no 4). Construir un 1205 real es el harness pendiente desde 1.5/1.6 (`SqlException` sin ctor público). `[tests/Reservas.IntegrationTests]`
- **Reserva con `AgenteEmail` null irresoluble por el atajo (403)** (edge, BAJA) — mismo fail-closed ya registrado para 4.2/3.3; backfill de `AgenteEmail` al desplegar sobre datos legacy. `[Reservas.Application/.../CancelarEnUnPasoCommandHandler.cs]`

## Deferred from: code review of story-4.2 (2026-07-09)

- **Re-solicitar cancelación tras un rechazo sobrescribe la auditoría del episodio en la owned** (edge, MED) — al rechazar, la reserva vuelve a `Confirmada`; una nueva `SolicitarCancelacion` reemplaza la owned `SolicitudCancelacion` por una instancia nueva (campos de resolución en null), perdiendo en BD quién/cuándo/por qué del rechazo previo. Es el diseño **single-episode** de la Task 0 de 4.1 ("un solo episodio, no dos owned"). El audit **durable** del rechazo es el evento `SolicitudCancelacionRechazada.v1` ya emitido al outbox (fuente de verdad del historial). Si el negocio exige historial de episodios en BD, es una historia propia (tabla `EpisodiosCancelacion`). `[Reservas.Domain/Reservas/Reserva.cs, SolicitudCancelacion.cs]`
- **Reserva con `AgenteEmail` null es irresoluble (403 permanente)** (edge, BAJA) — `Reserva.Crear` permite `agenteEmail` null; el aislamiento del resolutor compara `reserva.AgenteEmail == agente` → una reserva sin agente da 403 a todos, con mensaje "pertenece a otro agente" (engañoso). Fail-closed correcto e inocuo en greenfield (3.3 siempre setea el agente vía validador). Al desplegar sobre datos legacy, backfill de `AgenteEmail` (misma deuda que 3.3). `[Reservas.Application/.../ResolverCancelacionCommandHandler.cs]`
- **Sin test a nivel HTTP del endpoint de resolución** (auditor, BAJA) — los tests de integración envían el `Command` vía `ISender`; el binding JSON del enum `Decision` y los status 200/400/409/403 del endpoint `POST /api/v1/reservas/{id}/cancelacion/resolucion` no se aseveran end-to-end. Misma deuda de wiring HTTP transversal (FunctionalTests). `[tests/Reservas.IntegrationTests/ResolverCancelacionTests.cs]`

## Deferred from: code review of story-4.1 (2026-07-09)

- **Concurrencia optimista en la solicitud: 500 en vez de 409** (blind+edge, MED) — dos `SolicitarCancelacion` concurrentes sobre la misma reserva `Confirmada` cargan y pasan el guard; la perdedora en `SaveChanges` lanza `DbUpdateConcurrencyException` (InnerException null, no `SqlException`), que `EjecutorTransaccional` NO traduce (solo maneja 2627/2601 y deadlock 1205) ni reintenta → escapa como excepción no-negocio → 500. Semánticamente es una cancelación duplicada (debería ser 409). El evento de la tx perdedora sí se revierte (misma tx), sin evento fantasma. **Diferido a Story 4.2**: Task 0 asigna la arbitración de concurrencia/doble-liberación (rowVersion + guard → 409) a 4.2; el fix (traducir `DbUpdateConcurrencyException` a un `ExcepcionNegocio`/Conflicto en `EjecutorTransaccional`) es infra compartida por todos los comandos y necesita su propio test G1. `[Reservas.Infrastructure/Persistencia/EjecutorTransaccional.cs]`
- **"Hoy" en UTC vs fecha calendario de la estancia** (blind+edge, MED) — el handler resuelve `fechaSolicitud = DateOnly.FromDateTime(reloj.GetUtcNow().UtcDateTime)`; cerca de medianoche en zonas alejadas de UTC, el "hoy" UTC puede caer un día antes/después del día local del hotel, invirtiendo el borde de 30/29 días (0%↔100%) o la elegibilidad (día de entrada). Convención UTC+`DateOnly` de TODO el sistema (pre-existente: `HuespedDtoValidator` ya usa `DateOnly.FromDateTime(DateTime.UtcNow)`). Cerrar cuando se introduzca zona horaria del hotel/tenant como concepto de dominio. `[Reservas.Application/.../SolicitarCancelacionCommandHandler.cs]`
- **~~Autorización: IDOR + `Iniciador` autodeclarado~~ → CERRADO en Story 6.3** — el IDOR (cargar la reserva por `id` sin filtrar por agente) quedó cerrado: los 3 endpoints de cancelación filtran por `IContextoAgente` (reserva ajena → 404). El `Iniciador` autodeclarado se resolvió **por análisis**: el guard de aislamiento impide que un no-dueño alcance el endpoint, y forzar `Iniciador==rol` rompería FR-14 (agente en nombre del viajero) — es metadato de auditoría del agente autorizado, sin contradicción posible. Sin deuda residual. `[cerrado: SolicitarCancelacionCommandHandler.cs + review 6.3]`
- **Sin test a nivel HTTP del endpoint** (auditor, BAJA) — los tests de integración envían el `Command` vía `ISender`, no por el pipeline HTTP; el binding JSON y los status 200/400/409 del endpoint `POST /api/v1/reservas/{id}/solicitud-cancelacion` no se aseveran end-to-end (el 409 se infiere del tipo base `ExcepcionNegocio`). Misma deuda de wiring HTTP ya registrada para Hoteles/Reservas; abordar con un proyecto de FunctionalTests transversal. `[tests/Reservas.IntegrationTests/SolicitudCancelacionTests.cs]`

## Deferred from: code review of story-3.3 (2026-07-09)

- **Reservas con `AgenteEmail` NULL invisibles en el listado (backfill de datos previos)** (edge, BAJA-MEDIA) — el aislamiento filtra `r.AgenteEmail == @agente`; una reserva con `AgenteEmail` NULL (creada antes de 3.3, o vía `Reserva.Crear` sin agente) evalúa `NULL == @agente` = UNKNOWN → no aparece para nadie. Es fail-closed correcto e inocuo en greenfield (no hay reservas pre-3.3; en producción el validador exige `AgenteEmail`). Al desplegar sobre datos existentes con reservas sin agente, hacer backfill del `AgenteEmail`. `[Reservas.Infrastructure/Proyeccion/LectorReservasAgenteSql.cs]`
- **Listado de reservas sin paginación ni cota** (edge, BAJA) — `LectarReservasAgenteSql.ListarAsync` hace `ToListAsync` de TODAS las reservas del agente, sin `Take`/paginación. Fuera del alcance del AC-E3.3.1 (no pide paginación). Añadir paginación (`skip/take` o cursor) cuando el volumen histórico por agente lo justifique. `[Reservas.Infrastructure/Proyeccion/LectorReservasAgenteSql.cs, Reservas.Api/Program.cs]`

## Deferred from: code review of story-3.2 (2026-07-09)

- **Estado de hotel que NO llega por un evento de toggle (backfill + creación-deshabilitada)** — la dimensión `ProyeccionHotelEstado` se alimenta SOLO de `HotelHabilitado/HotelDeshabilitado`, que Hoteles emite en la transición de estado (`CambiarEstadoHotel`). El cierre de AC-E3.2.2 está probado para el **ciclo de toggle** (crear habilitado → deshabilitar → rehabilitar). Quedan dos entradas de estado que NO pasan por un toggle y por eso no propagan (la fila ausente se trata como activo → sus habitaciones se ofertarían):
  1. **Backfill (RELEASE, no greenfield):** hoteles ya deshabilitados ANTES del deploy no tienen historia de eventos. Inocuo en greenfield (BD de cero por migraciones). Al desplegar sobre datos existentes: emitir `HotelDeshabilitado.v1` (con `Version` correcta) por cada hotel deshabilitado, o sembrar `ProyeccionHotelEstado` desde el estado actual de Hoteles.
  2. **Creación deshabilitada:** `CrearHotel` acepta `Estado` (el validador permite `Deshabilitado`) y su escritura es un INSERT auto-contenido SIN outbox (2.1); un hotel creado deshabilitado no emite evento de estado. Cerrarlo requiere que `CrearHotel` emita `HotelDeshabilitado.v1` cuando `Estado==Deshabilitado` (tocando el camino transaccional de 2.1) o forzar que el alta sea siempre `Habilitado` y el estado se gestione solo por toggle. Decisión de diseño (amerita party-mode/historia propia; no se expandió el alcance de 3.2 una tercera vez). `[Hoteles.Application/Hoteles/CrearHotel, CambiarEstadoHotel; Reservas.Infrastructure/Proyeccion/ProyeccionHotelEstado.cs]`
- **Eventos LWW no-op rotan el token de caché igualmente** (auditor, BAJA) — en `ProyectorCatalogo.AplicarAsync`, los casos `PrecioHabitacionCambiado`/`HabitacionDeshabilitada` devuelven la fila afectada aunque `AplicarPrecio`/`AplicarDeshabilitada` descarten el evento por versión vieja/igual (LWW no cambió nada); eso rota el token de la ciudad y provoca misses de caché extra sin cambio real. Solo eficiencia (correcto funcionalmente). Fix: que los métodos del CRDT reporten si mutaron y solo invalidar cuando hubo cambio. `[Reservas.Infrastructure/Proyeccion/ProyectorCatalogo.cs, ProyeccionHabitacion.cs]`

## Deferred from: code review of story-1.1 (2026-07-08)

- **`gitleaks-action@v2` y repos de organización** — la acción es gratuita en cuentas personales; en un repo propiedad de una GitHub Organization exige `GITLEAKS_LICENSE`. Hoy el repo es personal (funciona). Si se mueve a un org, o migrar a invocar el binario/imagen de gitleaks directamente (sin la action), o proveer la licencia. `[.github/workflows/ci.yml]`
- **`depends_on` sin `condition: service_healthy` + infra sin healthchecks** — hoy inocuo (los servicios del esqueleto no conectan a SQL/Redis/RabbitMQ al arrancar). En cuanto un servicio abra conexión/migración en el arranque (Story 1.5+), el orden de cold-start puede provocar crashes en el primer boot. Sembrar healthchecks de infra + `condition: service_healthy` entonces. `[deploy/docker-compose.yml]`
- **Smoke test con confianza parcial** — el smoke valida `/health` de los 4 servicios, que reportan `Healthy` aunque las BD/broker no arranquen (no los consumen aún). Aceptable como límite del gate en el esqueleto; reforzar (healthchecks de infra / prueba de conectividad) cuando haya lógica de negocio que dependa de ellos. `[.github/workflows/ci.yml, deploy/docker-compose.yml]`

## Deferred from: code review of story-1.4 (2026-07-08)

- **Precio: clase concreta `CalculadorPrecio` vs `IEstrategiaPrecio` (Strategy)** — `patterns.md` describe el precio como estrategia extensible (temporada alta/baja, descuento por estancia larga), pero `architecture.md` lo fija como domain service puro concreto para el alcance actual (solo base+impuesto). YAGNI ahora; si entran múltiples estrategias de precio, refactorizar hacia interfaz + inyección. `[Reservas.Domain/Servicios/CalculadorPrecio.cs]`

## Deferred from: code review of story-1.5 (2026-07-08)

- **[Ampliado en 1.6c]** El test de integración con **1205 forzado** debe cubrir también el mapeo "1205 agotado → 409" del `EjecutorTransaccional` (hoy sin aserción: la contención de fila única del money test produce 2627, no 1205). Mismo test cierra la cobertura de `PoliticaReintentos.EsDeadlock` sobre `SqlException`/`DbUpdateException`.
- **Retry 1205 no cableado + `PoliticaReintentos.EsDeadlock` sin cobertura E2E → 1.6b** — la política existe y está unit-testeada con un predicado sintético, pero no se compone con `ConfirmarAsync` (por diseño: el retry vive en el `TransactionBehavior` del pipeline del Mediator, que llega con 1.6). El predicado por defecto `EsDeadlock` (desenvuelve `SqlException`/`DbUpdateException`) no se puede unit-testear cómodamente porque `SqlException` no tiene ctor público → su cobertura real debe venir de un **test de integración con 1205 forzado** en 1.6b. Al cablear, **crear un `DbContext`/scope nuevo por intento** (no reusar el grafo en estado `Added` tras el fallo). `[Reservas.Infrastructure/Persistencia/ReservaRepository.cs, PoliticaReintentos.cs]`
- **Guarda de longitud máxima de estancia → 1.6a** — `Estancia.Crear` (Story 1.4) solo valida `salida > entrada`; sin tope superior, `Reserva.Crear` genera un `NocheHabitacion` por noche → una estancia de años produce cientos de miles de slots en una sola transacción (escalado de bloqueos en `NochesHabitacion`, memoria del ChangeTracker, command timeout). Añadir tope máximo de noches en la validación de `CrearReservaCommand`. `[Reservas.Domain/Reservas/Estancia.cs, CrearReservaCommand validation]`

## Deferred from: code review of story-1.6a (2026-07-08)

- **Persistir huéspedes/contacto de emergencia en el agregado → 1.6b** — 1.6a valida los datos (FR-10/11) y los lleva al evento, pero NO los mapea en EF ni los persiste en `Reserva` (habría requerido mapeo + migración sin test de integración en 1.6a, que es unit-only). Cuando 1.6b endurezca la escritura transacional (outbox en la misma tx), añadir el mapeo EF (owned collection de `Huespedes` + owned `ContactoEmergencia`), su migración y el test de integración. `[Reservas.Domain/Reservas/Reserva.cs, ReservasDbContext]`
- **Guarda de longitud máxima de estancia (pendiente todavía)** — la deuda registrada en 1.5 sigue abierta: 1.6a no añadió el tope de noches en el validator. Cerrarla al implementar 1.6a-hardening o 1.6c. `[CrearReservaCommandValidator]`
- **Placeholders de 1.6a** — `DisponibilidadHabitacionSembrada` (trata toda habitación como activa) → reemplazar por la proyección real en E3; `PublicadorEventosLog` (solo loguea) → reemplazar por el relay del outbox→Dapr en 1.6b. `[Reservas.Infrastructure/Disponibilidad, Reservas.Infrastructure/Mensajeria]`
- **`ToCreatedResult` en `Reservas.Api` → proyecto web transversal** — la extensión Result→HTTP debería ser única para todos los BCs; hoy vive en `Reservas.Api` para no crear un `Comun.Web` prematuro. Promoverla cuando aparezca el 2º BC con Api (E2). `[Reservas.Api/Http/ResultadoHttpExtensions.cs]`
- **`Sender` robusto ante handlers no-`async` → 1.6b** — compone el pipeline por reflexión (`MethodInfo.Invoke`); un handler/behavior no-`async` que lance síncrono saldría envuelto en `TargetInvocationException`. Latente (convención: todos los handlers son `async`). Al añadir el `TransactionBehavior`, desenvolver `InnerException`. `[Comun/Mensajeria/Sender.cs]`
- **Capacidad vs nº de huéspedes → E3** — el handler recibe `Capacidad` pero no la contrasta con `Huespedes.Count`. Aplicar al aterrizar el catálogo/proyección real. `[CrearReservaCommandHandler.cs]`
- **Naming vs lista cerrada de sufijos → E-T** — `Sender`/`ISender` (contrato del mediator ADR-018), `PublicadorEventosLog` (sufijo `Log`) y `ManejadorExcepcionesNegocio` (traduce el sufijo `Handler`) no están en la lista cerrada. Whitelistear los términos del mediator en `AGENTS.md` y renombrar/eliminar los placeholders cuando se construya el check de sufijos en CI. `[Comun/Mensajeria, Reservas.Infrastructure/Mensajeria, Reservas.Api/Http]`

## Deferred from: bmad-code-review formal de la Épica 2 (2026-07-09)

- **Fail-fast de la connection string en ambos BC → E-T / hardening de infraestructura** — `AddHotelesInfrastructure`/`AddReservasInfrastructure` hacen `UseSqlServer(cadena)` sin validar que la cadena no sea null; una clave mal nombrada o config ausente levanta la app sana y solo revienta con 500 en el primer request (fail-slow). Añadir `ArgumentException.ThrowIfNullOrWhiteSpace(cadena)` para fallar en el arranque. Aplica a los dos BC → hacerlo consistente. `[Hoteles.Infrastructure/RegistroInfraestructura.cs, Reservas.Infrastructure/RegistroInfraestructura.cs]`
- **Aceptar el enum por nombre en JSON (JsonStringEnumConverter) → E-T / usabilidad de la API** — hoy `Estado`/`EstadoObjetivo` se bindean solo numéricos (STJ por defecto); un cliente que envíe `"Habilitado"` recibe 400 de deserialización. Registrar `JsonStringEnumConverter` es transversal a toda la API; decidir de una vez el contrato del enum en el JSON. `[*.Api/Program.cs]`
- **Guardia de fuga en el handler transversal → cuando haya subtipos de ExcepcionNegocio con mensajes derivados** — `ManejadorExcepcionesNegocio` refleja `ExcepcionNegocio.Message` al cliente. Hoy seguro (mensajes curados en `ConflictoConcurrenciaException`/`HabitacionNoDisponibleException`), pero un subtipo futuro construido con `new X(exInterna.Message)` filtraría internals. Al añadir nuevos subtipos, revisar que el `Message` sea siempre seguro para el cliente (o separar mensaje-cliente de mensaje-log). `[Comun.Web/ManejadorExcepcionesNegocio.cs]`

## Deferred from: code review of story-2.4 (2026-07-09)

- **Habitación huérfana por race hotel-eliminado (check-then-act sin FK) → E3 / consumo** — `CrearHabitacion` verifica que el hotel exista y luego inserta en otro `SaveChanges`; sin FK constraint (independencia de aggregates), si el hotel se elimina justo en medio, la habitación queda apuntando a un hotel invisible. Impacto bajo (la proyección/búsqueda de E3 filtra por hotel activo). Trade-off asumido: NO forzar FK (acoplaría los aggregates); resolver en el consumo/proyección de Reservas. Documentado en el docstring de `CrearHabitacionCommandHandler`. `[Hoteles.Application/Habitaciones/CrearHabitacion]`

## Deferred from: code review of story-2.3 (2026-07-09)

- **Tests de wiring HTTP de los endpoints de Hoteles (WebApplicationFactory) → E-T / endurecimiento de pruebas** — ningún slice de Hoteles (crear/editar/eliminar/habilitar/deshabilitar) prueba su endpoint extremo a extremo: la lógica está cubierta por unit (handler/validator/mapeo) + integración (repo/concurrencia real), pero el wiring (ruta, verbo, `comando with { Id = id, EstadoObjetivo = ... }` que fija el estado por ruta, `UseExceptionHandler`→Problem Details) no. Se quitó `public partial class Program` deliberadamente para no arrastrar infra no usada. Introducir `WebApplicationFactory` es una decisión transversal a TODA la API (afecta 2.1–2.5): abordarla de una vez con un proyecto `Hoteles.FunctionalTests` que valide status/headers/shape end-to-end. Riesgo actual bajo (los overrides son literales, no lógica que regresione sutilmente). `[Hoteles.Api/Program.cs]`

## Deferred from: code review of story-2.2 (2026-07-09)

- **Concurrencia por `ETag`/`If-Match` (transporte del rowVersion) → E3 (cuando exista GET)** — decisión de party-mode (Winston/Amelia). Hoy el `rowVersion` viaja como campo `byte[]` (base64) en el body de PUT y DELETE — una sola convención, mínima y simétrica. El transporte HTTP-idiomático es `If-Match` con el `ETag` que emitiría un GET; introducirlo AHORA (sin GET/ETag) crearía una asimetría a medias (rowVersion sale por body, entra por cabecera). Migrar de forma global y deliberada cuando E3 añada GET/listado con `ETag`: emitir `ETag` en las respuestas, leer `If-Match` en PUT/DELETE (→ 428 si falta), y dejar el DELETE sin body (RFC 9110: el body en DELETE es no estándar y algún proxy lo descarta). `[Hoteles.Api/Program.cs, HotelResponseDto]`
- **Transición de estado del hotel (habilitar/deshabilitar) es exclusiva de 2.3** — el PUT de editar NO cambia `Estado` (decisión party-mode): el ciclo de vida tendrá su operación dedicada con sus reglas (¿deshabilitar con reservas activas?). Confirmar en 2.3 que no queda un segundo camino para mutar el estado. `[Hoteles.Application/Hoteles/EditarHotel]`

## Deferred from: code review of story-1.6b (2026-07-08)

- **Outbox multi-evento por comando → cuando exista un comando que lo requiera** (decisión de party-mode: Winston/Amelia/Murat). Hoy el `MessageId` es por-comando y es la columna `UNIQUE(OutboxMessages.MessageId)`; un comando con ≥2 eventos colisionaría. Contenido con un guard en `ColaOutbox.Encolar` (lanza `InvalidOperationException`, no 409) + contrato "1 evento/comando". El **arreglo estructural** cuando llegue el primer comando multi-evento: (1) `MessageId` por-MENSAJE estable entre reintentos (ordinal determinista sembrado por el MessageId del comando, re-sembrado por intento junto al `ChangeTracker.Clear()`); (2) **desacoplar la identidad del mensaje de la clasificación** — que una violación de `UNIQUE(MessageId)` NO se traduzca a `HabitacionNoDisponibleException`/409 (hoy imposible discriminar por `Number` sin parsear el mensaje; requerirá separar el árbitro de dominio del índice de outbox, p. ej. constraint dedicada verificable de otra forma). `[Reservas.Infrastructure/Mensajeria/ColaOutbox.cs, Persistencia/EjecutorTransaccional.cs]`

## Deferred from: code review of story-2.5 (2026-07-09)

- **Migración `Version=0` en habitaciones preexistentes → sin `HabitacionAgregada` retroactiva** (blind+edge+auditor, MED) — la migración `AgregaVersionYOutboxHabitaciones` fija `Version` con `defaultValue: 0` para filas ya existentes de 2.4. Como esas habitaciones nunca emitieron `HabitacionAgregada`, su primera mutación emisora post-migración produciría un `PrecioHabitacionCambiado`/`HabitacionDeshabilitada` sin el alta previa → la proyección de E3 no las conocería. Inocuo en greenfield (la BD se crea de cero por migraciones; no hay filas de 2.4 comprometidas antes de 2.5). Al desplegar sobre datos existentes, hacer un **backfill que emita `HabitacionAgregada` (con la Version correcta) para toda habitación preexistente** antes de habilitar el catálogo en E3. `[Hoteles.Infrastructure/Migraciones/*AgregaVersionYOutboxHabitaciones.cs]`
- **Sin dead-letter ni tope de intentos en el relay (mensaje-veneno)** (blind+edge, MED→LOW) — un mensaje que falle SIEMPRE al publicar (broker roto persistente o payload corrupto: `Deserialize` está dentro del `try`) queda `Pendiente` y se re-reclama en cada vencimiento de lease sin cota; `Intentos` crece sin umbral. Paridad con el outbox de Reservas 1.6b (mismo límite operativo). Al endurecer el relay (o al promoverlo a `Comun` en la regla de tres), añadir tope de intentos + dead-letter + backoff. `[Hoteles.Infrastructure/Outbox/ProcesadorOutbox.cs]`
- **Reclamo del outbox no atómico (doble publicación con múltiples instancias del relay)** (blind+edge, LOW/informativo) — el patrón read-then-claim (`SELECT` pendientes → `Reclamar` + `SaveChanges`) tiene ventana de carrera si corren dos instancias del `RelayOutbox`. Por diseño es at-least-once (`deliveries >= 1`, dedup por `MessageId` en el consumidor E5) con despliegue de instancia única; si se escala a N réplicas, añadir claim atómico (`UPDATE ... OUTPUT` con token/lease) o coordinación. `[Hoteles.Infrastructure/Outbox/ProcesadorOutbox.cs]`

## Deferred from: Story 3.1 Task 0 (party-mode, 2026-07-09)

- **Reconciliación de atributos de catálogo vs Hoteles (snapshot API)** (party-mode D4) — la reconciliación de 3.1 es un recompute idempotente LOCAL (disponibilidad desde `NochesHabitacion`). Los atributos de catálogo (hotel, ciudad, tipo, costo, impuestos, capacidad, activa) solo provienen de eventos de Hoteles; no hay event-store durable cross-BC ni snapshot API para reconstruirlos si la proyección se corrompe. Cuando se necesite corregir catalog-drift, Hoteles debe exponer un **snapshot query** ("estado actual de habitaciones") que el job de rebuild de Reservas consuma. `[Reservas.Infrastructure/Proyeccion, Hoteles.Api]`
- **Inbox de E5 (Notificaciones) por efecto externo** (party-mode D3, matiz de AC-E3.1.4) — el "único inbox compartido" es el CONCEPTO (dedup por `MessageId`), no un único store. E3 (proyección con upsert SQL) mete la dedup en la MISMA transacción SQL (autoridad dura). E5 (envío de email, efecto externo no transaccionable con SMTP) usará `Redis SETNX+TTL` como dedup best-effort at-least-once. Al implementar E5, reutilizar el patrón/abstracción de inbox pero con el store adecuado al efecto; NO forzar una tabla SQL para un efecto no transaccional. `[Notificaciones.Worker, Comun/Idempotencia]`

## Deferred from: Story 3.1 implementación (2026-07-09)

- **Reemplazar `DisponibilidadHabitacionSembrada` por la proyección real** — el placeholder de 1.6a (todo activo, datos demo) alimenta `CrearReserva` vía `IDisponibilidadHabitacion`→`HabitacionReservable`, que necesita `HotelNombre`. El read-model `ProyeccionHabitacion` (3.1) NO lleva `HotelNombre` (el contrato `HabitacionAgregada.v1` tampoco) — gap análogo al de `ciudad`. Al conectar la proyección al flujo de reserva, añadir `HotelNombre` al contrato (aditivo) y mapear el puerto contra el read-model. 3.2 (búsqueda) ya consume la proyección directamente sin este puerto. `[Reservas.Infrastructure/Disponibilidad, Comun.Eventos/HabitacionAgregadaV1]`
- **AC-E3.1.3 recompute desde fuente (reconciliación real)** — la idempotencia del proyector (re-aplicar converge) está probada, pero un rebuild de una proyección corrupta necesita re-obtener los atributos de catálogo, que solo vienen de eventos de Hoteles; sin event-store cross-BC ni snapshot API, no es reconstruible localmente. Implementar cuando Hoteles exponga un snapshot query. `[Reservas.Infrastructure/Proyeccion, Hoteles.Api]`

## Deferred from: code review of story-3.1 (2026-07-09)

- **Endurecimiento competing-consumers de `ProyeccionHabitacion` (token de concurrencia)** (blind+edge, ALTA) — el compare-and-set de las guardas LWW vive en C# (read-modify-write en `ProyectorCatalogo`), no en SQL. Con un ÚNICO consumidor por partición (lo que hoy garantiza el productor 2.5: outbox head-of-line single-instance) es correcto y converge. Si se escala a competing-consumers (Dapr pub/sub con N réplicas del consumidor), dos updates concurrentes sobre el mismo agregado pueden regresar la versión (lost update). Endurecer con `RowVersion`/`IsConcurrencyToken` en `ProyeccionHabitacion` + retry, o `UPDATE ... WHERE VersionPrecio < @v` (compare-and-set atómico en SQL). `[Reservas.Infrastructure/Proyeccion/ProyeccionHabitacion.cs, ProyectorCatalogo.cs]`
- **Índice de búsqueda filtrado por `Hidratada`** (edge, LOW) — `IX_ProyeccionHabitacion_Ciudad` no acota filas parciales (estáticos NULL por un delta-antes-del-alta). Al construir la búsqueda de 3.2, filtrar `WHERE VersionEstatico IS NOT NULL` (índice filtrado) para no considerar filas no hidratadas. `[Reservas.Infrastructure/Persistencia/ReservasDbContext.cs]`
